<h1>All Products</h1>
<ul>
	<% @products.each do |product| %>
		<li><%= link_to product.name, product_path(product) %></li>
	<% end %>
</ul>

<%= button_to "Create New Product", "/products/new", method: :get, class: "btn btn-primary btn-md" %>