<h1>All Categories</h1>
<ul>
	<% @categories.each do |category| %>
		<li><%= link_to category.name, category_path(category) %></li>
	<% end %>
</ul>

<%= button_to "Create New Category", "/categories/new", method: :get, class: "btn btn-primary btn-md" %>